#include "utils.h"


/*
vector<Command> parse(string command){
    string temp_cmd;
    int temp_cmd_index;
    string temp_content;
    int temp_cmd_index;
    bool cmd;
    for(int i = 0;  i < command.length(); i++){
        if(command[i] != ' '){
            temp_cmd[temp_cmd_index] = command[i];
            temp_cmd_index++;
        } else {
            if(temp_cmd == "fd"){

            }
        }
    }
}
*/